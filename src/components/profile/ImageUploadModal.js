import React from 'react';
import Modal from '../common/Modal';

const ImageUploadModal = (props) => (
  <Modal visible={false} title={'프로필'} closeButton={false} />
);

export default ImageUploadModal;
